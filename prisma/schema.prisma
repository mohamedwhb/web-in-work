generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters", "strictUndefinedChecks"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
